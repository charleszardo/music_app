<h1><%= @album.title %></h1>
<% @album.tracks.each do |track| %>
  <p><%= link_to track_url(track) %></p>
<% end %>
<%= link_to "Add Track", new_track_url %>
<%= link_to @album.band_name, band_url(@album.band) %>
